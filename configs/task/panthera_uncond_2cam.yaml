# @package _global_

defaults:
  - override /data: panthera_2cam
  - override /model: fastwam
  - _self_

batch_size: 1
num_workers: 4
mixed_precision: bf16
gradient_accumulation_steps: 1
learning_rate: 1.0e-4
weight_decay: 1.0e-2
num_epochs: 10
max_steps: null
log_every: 10
save_every: 2000
eval_every: 200

model:
  mot_checkpoint_mixed_attn: true
